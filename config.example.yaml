auth:
  email: "your@email.com"
  name: "Иван Иванов"  # Имя для подписи в сопроводительных письмах (опционально)

browser:
  profile_dir: "./data/browser_profile"
  headless: false      # false обязателен для patchright

search:
  query: ""            # пусто = спросить при запуске
  area_id: 113         # Регион поиска (см. ниже коды стран)
  # area_ids: [113, 16, 40]  # Несколько стран одновременно (Россия, Беларусь, Казахстан)
  max_pages: 5         # страниц выдачи за сессию

# 📍 КОДЫ СТРАН И РЕГИОНОВ:
#   113 - Россия (вся)
#   16  - Беларусь
#   40  - Казахстан
#   1   - Москва
#   2   - Санкт-Петербург
#   88  - Казань
#   62  - Новосибирск
#   126 - Екатеринбург
#   143 - Краснодар
# Для нескольких стран используйте area_ids: [113, 16, 40]

limits:
  max_applications_per_session: 20
  min_delay_between_applications: 10   # секунды
  max_delay_between_applications: 30

filters:
  skip_with_tests: true
  skip_direct_vacancies: true
  blocked_keywords: []
  blocked_employers: []

cover_letter:
  enabled: false
  always_include: false
  template: |
    Добрый день! Меня заинтересовала вакансия {vacancy_name} в компании {company_name}.
    Готов обсудить детали.
  
  # AI-генерация сопроводительных писем
  ai:
    enabled: false                    # true = использовать AI для генерации писем
    provider: "auto"                  # Провайдер: "openrouter", "groq", или "auto" (попробовать все)
    api_key: ""                       # API ключ (OpenRouter или Groq)
    
    # Модели для OpenRouter (provider: openrouter или auto):
    #   - mistralai/mistral-7b-instruct:free    # Mistral 7B (устаревшая бесплатная)
    #   - meta-llama/llama-3.1-8b-instruct:free # Llama 3.1 8B (устаревшая бесплатная)
    #   - openai/gpt-4o-mini                    # Платная (~$0.0001 за запрос)
    #   - anthropic/claude-3-haiku              # Платная
    # 
    # Для Groq (provider: groq или auto):
    #   - llama-3.1-8b-instant  # Бесплатный tier: 15 запросов/мин, 14400/день
    #   - mixtral-8x7b-32768    # Бесплатный tier: 10 запросов/мин, 7200/день
    #   Получить ключ: https://console.groq.com/
    model: "mistralai/mistral-7b-instruct:free"
    max_tokens: 500
    temperature: 0.7
    custom_prompt: ""                 # Кастомный системный промпт (если пусто - используется дефолтный)

resume:
  preferred_title: ""  # часть названия резюме, пусто = первое
