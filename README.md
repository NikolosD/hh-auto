# 🤖 hh.ru Auto-Apply Bot

Автоматический бот для откликов на вакансии hh.ru с AI-генерацией сопроводительных писем на основе вашего резюме.

## ✨ Возможности

- 🔐 **Умная авторизация** — несколько способов входа (config, --email, интерактивный)
- 🤖 **AI-сопроводительные письма** — автоматическая генерация на основе вашего резюме
- 🌍 **Работа с любыми вакансиями** — поддержка вакансий в других странах (обрабатывает модалку "Вы откликаетесь в другой стране")
- 🚫 **Пропуск откликнутых** — не тратит время на вакансии, на которые уже откликнулись
- 📊 **Статистика** — отслеживание всех откликов и пропусков
- ⚙️ **Гибкие фильтры** — пропуск вакансий с тестами, по ключевым словам и т.д.

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонировать репозиторий
git clone https://github.com/NikolosD/hh-auto.git
cd hh-auto

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка

Скопируйте пример конфига и отредактируйте:

```bash
copy config.example.yaml config.yaml
```

Отредактируйте `config.yaml`:

```yaml
auth:
  email: "your-email@example.com"  # ваш email на hh.ru

search:
  query: "Python разработчик"      # поисковый запрос
  area_id: 113                      # 113 = Россия, 1 = Москва, 2 = СПб
  # area_ids: [113, 16, 40]         # несколько стран: Россия, Беларусь, Казахстан
  max_pages: 5                      # сколько страниц обработать

limits:
  max_applications_per_session: 20  # макс. откликов за сессию
  min_delay_between_applications: 10  # мин. задержка (сек)
  max_delay_between_applications: 30  # макс. задержка (сек)

# AI-сопроводительные письма
cover_letter:
  enabled: true                     # включить генерацию писем
  ai:
    enabled: true                   # использовать AI (GPT/DeepSeek)
    provider: "deepseek"            # или "openai"
    api_key: ""                     # ваш API ключ (опционально)
```

### 3. Авторизация

**Способ 1: Через config.yaml (как раньше)**
```bash
py main.py login
```

**Способ 2: Быстрый вход с указанием email**
```bash
py main.py login --email your-email@example.com
# или
py main.py login -e your-email@example.com
```

**Способ 3: Интерактивный ввод (удобно для смены аккаунтов)**
```bash
py main.py login --interactive
# или
py main.py login -i
```

После запуска откроется браузер, введите код из письма — сессия сохранится.

### 4. Запуск откликов

**Пробный прогон (без реальных откликов):**
```bash
py main.py run --query "Frontend разработчик" --dry-run
```

**Реальный запуск:**
```bash
# Использовать запрос из config.yaml
py main.py run

# Или указать запрос в командной строке
py main.py run --query "Python разработчик"
py main.py run --query "Vue Frontend" --area 1  # Москва
```

### 🌍 Поиск в нескольких странах

**Поддерживаемые коды стран:**
- `113` — Россия (вся)
- `16` — Беларусь
- `40` — Казахстан
- `1` — Москва
- `2` — Санкт-Петербург

**Поиск в нескольких странах одновременно:**

```yaml
# В config.yaml:
search:
  area_ids: [113, 16, 40]  # Россия + Беларусь + Казахстан
```

Или через командную строку (одна страна):
```bash
py main.py run --query "Frontend" --area 16   # Беларусь
py main.py run --query "Python" --area 40     # Казахстан
```

## 📋 Команды

### `login` — Авторизация

```bash
py main.py login [OPTIONS]

Options:
  -e, --email TEXT       Email для входа
  -i, --interactive      Интерактивный ввод email
  -c, --config PATH      Путь к config.yaml
```

### `run` — Запуск сессии откликов

```bash
py main.py run [OPTIONS]

Options:
  -q, --query TEXT       Поисковый запрос
  --dry-run              Пробный прогон без откликов
  --area INTEGER         Регион (1=Москва, 2=СПб, 113=Россия)
  -c, --config PATH      Путь к config.yaml
```

### `status` — Статистика откликов

```bash
py main.py status
```

Показывает:
- Общее количество откликов
- Количество пропущенных вакансий
- Последние 10 откликов

### `clear` — Очистка базы данных

```bash
py main.py clear
```

Удаляет всю историю откликов (требует подтверждения).

## ⚙️ Конфигурация

### Фильтры вакансий

```yaml
filters:
  skip_with_tests: true           # пропускать вакансии с тестами
  skip_direct_vacancies: true     # пропускать прямые вакансии
  blocked_keywords:               # пропускать если в заголовке есть:
    - "Senior"                    # например, не откликаться на Senior позиции
    - "Team Lead"
  blocked_employers:              # пропускать работодателей:
    - "Название компании"
```

### AI-сопроводительные письма

Бот автоматически:
1. Загружает ваше резюме с hh.ru
2. Анализирует навыки и опыт
3. Генерирует персонализированное письмо для каждой вакансии

**Настройка AI:**
```yaml
cover_letter:
  enabled: true
  ai:
    enabled: true
    provider: "deepseek"          # или "openai"
    model: "deepseek-chat"        # или "gpt-3.5-turbo"
    api_key: "your-api-key"       # API ключ (опционально)
    max_tokens: 500
    temperature: 0.7
```

Если API ключ не указан — используется шаблон из конфига.

### Выбор резюме

Если у вас несколько резюме:
```yaml
resume:
  preferred_title: "Frontend"     # часть названия нужного резюме
```

## 🌍 Особенности работы

### Вакансии в других странах

Бот автоматически обрабатывает модалку "Вы откликаетесь на вакансию в другой стране":
1. Нажимает "Все равно откликнуться"
2. Продолжает процесс отклика
3. Добавляет сопроводительное письмо

### Уже откликнутые вакансии

Бот автоматически пропускает вакансии, на которые вы уже откликнулись:
- Проверяет наличие кнопки "Откликнуться"
- Ищет текст "Резюме доставлено" или "Вы уже откликнулись"
- Помечает как `already_applied` в статистике

### Скорость работы

```yaml
limits:
  max_applications_per_session: 20    # сколько откликов за раз
  min_delay_between_applications: 10  # мин. пауза (сек)
  max_delay_between_applications: 30  # макс. пауза (сек)
```

Бот делает паузу между откликами (случайная в диапазоне) и кофе-брейк каждые 5 откликов.

## 📁 Структура проекта

```
hh-auto/
├── main.py                    # Точка входа
├── config.yaml               # Конфигурация
├── config.example.yaml       # Пример конфига
├── requirements.txt          # Зависимости
├── hh_bot/
│   ├── auth/
│   │   └── login.py         # Авторизация
│   ├── bot/
│   │   ├── runner.py        # Основной цикл бота
│   │   ├── filters.py       # Фильтры вакансий
│   │   └── state.py         # База данных откликов
│   ├── browser/
│   │   ├── launcher.py      # Запуск браузера
│   │   └── human.py         # Имитация человеческих действий
│   ├── scraper/
│   │   ├── search.py        # Поиск вакансий
│   │   ├── vacancy.py       # Парсинг вакансий
│   │   ├── apply.py         # Отклик на вакансии
│   │   └── resume_parser.py # Парсинг резюме
│   └── utils/
│       ├── config.py        # Управление конфигом
│       ├── delays.py        # Задержки и паузы
│       └── logger.py        # Логирование
└── data/
    └── browser_profile/     # Профиль браузера (сессия)
```

## 🐛 Отладка

Запуск с подробным логом:
```bash
py main.py run --log-level DEBUG
```

## ⚠️ Важно

1. **Headless mode**: В конфиге `headless: false` обязателен для работы с капчей/подтверждениями
2. **Профиль браузера**: Сессия сохраняется в `data/browser_profile/`, не нужно логиниться каждый раз
3. **Задержки**: Не уменьшайте задержки слишком сильно — hh.ru может заблокировать

## 📝 Лицензия

MIT License — используйте на свой страх и риск.

---

**Автор:** NikolosD  
**GitHub:** https://github.com/NikolosD/hh-auto
